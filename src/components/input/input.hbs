<input
  ref="input"
  class="{{classType}}__input"
  id="{{id}}"
  name="{{name}}"
  type="{{type}}"
  {{#if value}}value="{{value}}"{{/if}}
  placeholder="{{placeholder}}"
  {{#if disabled}}disabled="{{disabled}}"{{/if}}
/>
